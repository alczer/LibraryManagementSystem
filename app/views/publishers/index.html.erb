<p id="notice"><%= notice %></p>

<h1>Wydawcy</h1>

<table>
  <thead>
    <tr>
      <th>Nazwa</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @publishers.each do |publisher| %>
      <tr>
        <td><%= publisher.name %></td>
        <td><%= link_to 'Pokaż', publisher %></td>
        <td><%= link_to 'Edytuj', edit_publisher_path(publisher) %></td>
        <td><%= link_to 'Usuń', publisher, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nowy wydawca', new_publisher_path %>
